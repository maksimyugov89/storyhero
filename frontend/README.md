# StoryHero App

Flutter приложение для создания персонализированных детских книг.

## 📱 О проекте

StoryHero — это кроссплатформенное мобильное приложение, позволяющее родителям создавать уникальные детские книги с персонализированными персонажами, сюжетами и иллюстрациями на основе характеристик ребенка (возраст, интересы, страхи, личность). Приложение использует AI для генерации текста и изображений, предоставляя готовую книгу в цифровом формате.

## 🏗️ Архитектура

### Структура проекта (Feature‑First)

```
lib/
├── app.dart                    # Главный виджет приложения
├── main.dart                   # Точка входа с инициализацией
├── secrets.dart                # Конфиденциальные ключи (не в репозитории)
├── app/
│   └── routes/
│       ├── app_router.dart     # Конфигурация GoRouter с защитой маршрутов
│       ├── route_names.dart    # Константы путей
│       └── route_transitions.dart
├── core/
│   ├── api/
│   │   ├── api_client.dart     # Настройка Dio с interceptors (JWT, 401 обработка)
│   │   ├── backend_api.dart    # API методы для backend
│   │   └── supabase_provider.dart
│   ├── auth/
│   │   ├── auth_status.dart    # Enum состояний аутентификации
│   │   └── auth_status_provider.dart
│   ├── models/                 # Модели данных (Freezed + JSON сериализация)
│   │   ├── child.dart          # Профиль ребенка (с полем gender)
│   │   ├── book.dart           # Книга
│   │   ├── scene.dart          # Сцена книги
│   │   ├── scene_variant.dart  # Варианты сцены
│   │   ├── task_status.dart    # Статус задачи генерации
│   │   ├── support_message.dart # Сообщение поддержки
│   │   ├── support_message_reply.dart # Ответ на сообщение поддержки
│   │   ├── user.dart           # Пользователь
│   │   └── ...
│   ├── presentation/
│   │   ├── design_system/      # Дизайн-система (Material 3)
│   │   │   ├── app_colors.dart
│   │   │   ├── app_radius.dart
│   │   │   ├── app_shadows.dart
│   │   │   ├── app_spacing.dart
│   │   │   ├── app_theme.dart  # Темная тема
│   │   │   └── app_typography.dart
│   │   ├── layouts/
│   │   │   ├── app_page.dart   # Базовая страница
│   │   │   └── app_scaffold.dart
│   │   └── widgets/            # Переиспользуемые виджеты
│   │       ├── buttons/
│   │       ├── cards/
│   │       ├── feedback/
│   │       ├── inputs/
│   │       └── navigation/
│   ├── services/
│   │   ├── deep_link_service.dart    # Обработка deep links
│   │   └── screenshot_protection_service.dart
│   ├── theme/
│   │   ├── app_theme.dart      # Основная тема
│   │   └── app_theme_magic.dart
│   ├── utils/
│   │   ├── error_analyzer.dart
│   │   ├── image_compressor.dart     # Сжатие изображений
│   │   ├── impeller_guard.dart       # Отключение Impeller на проблемных устройствах
│   │   ├── navigation_helpers.dart
│   │   ├── phone_formatter.dart      # Форматирование телефонных номеров
│   │   └── text_style_helpers.dart
│   └── widgets/
│       ├── error_widget.dart
│       ├── loading_widget.dart
│       ├── rounded_image.dart
│       ├── address_input_fields.dart # Поля ввода адреса
│       ├── phone_input_field.dart    # Поле ввода телефона
│       └── magic/              # Анимированные "магические" компоненты
│           ├── animated_gradient_background.dart
│           ├── flying_particles.dart
│           ├── glassmorphic_card.dart
│           ├── glowing_progress_bar.dart
│           ├── magic_background.dart
│           ├── magic_button.dart
│           ├── magic_icon_button.dart
│           ├── magic_input_field.dart
│           ├── magic_pen_animation.dart
│           ├── magic_text.dart
│           └── timeline_step.dart
├── features/                   # Функциональные модули
│   ├── auth/                  # Аутентификация
│   │   ├── data/
│   │   │   └── auth_repository.dart
│   │   └── presentation/
│   │       ├── welcome_screen.dart
│   │       ├── login_screen.dart
│   │       ├── register_screen.dart
│   │       └── loading_screen.dart
│   ├── children/              # Управление детьми
│   │   ├── data/
│   │   │   └── child_photos_provider.dart
│   │   ├── presentation/
│   │   │   ├── children_list_screen.dart
│   │   │   ├── child_create_screen.dart
│   │   │   ├── child_edit_screen.dart
│   │   │   └── child_profile_screen.dart
│   │   └── state/
│   │       └── child_photos_provider.dart
│   ├── books/                 # Просмотр и редактирование книг
│   │   ├── data/
│   │   │   ├── book_providers.dart
│   │   │   └── scene_variants_provider.dart
│   │   └── presentation/
│   │       ├── books_list_screen.dart
│   │       ├── book_view_screen.dart
│   │       ├── child_books_screen.dart
│   │       ├── edit_scene_screen.dart
│   │       ├── edit_text_screen.dart
│   │       ├── edit_text_with_variants_screen.dart
│   │       ├── edit_image_with_variants_screen.dart
│   │       ├── finalization_screen.dart
│   │       ├── book_finalize_screen.dart
│   │       ├── book_complete_screen.dart
│   │       └── book_order_screen.dart
│   ├── generate/              # Генерация книг
│   │   └── presentation/
│   │       ├── create_book_screen.dart
│   │       └── task_status_screen.dart
│   ├── home/                  # Главный экран
│   │   └── presentation/
│   │       └── home_screen.dart
│   ├── payments/              # Платежи
│   │   └── presentation/
│   │       └── payment_screen.dart
│   ├── settings/              # Настройки и поддержка
│   │   ├── data/
│   │   │   └── support_messages_provider.dart
│   │   └── presentation/
│   │       ├── settings_screen.dart
│   │       ├── help_screen.dart
│   │       └── support_message_detail_screen.dart
│   ├── subscription/          # Подписка
│   │   ├── data/
│   │   │   └── subscription_provider.dart
│   │   └── presentation/
│   │       └── subscription_screen.dart
│   └── splash/                # Заставка
│       └── presentation/
│           └── splash_screen.dart
└── ui/                        # UI компоненты (не связанные с бизнес-логикой)
    └── components/
        ├── animated_magic_input.dart
        ├── asset_icon.dart
        ├── glowing_capsule_button.dart
        ├── golden_book_logo.dart
        ├── golden_runic_spinner.dart
        ├── magic_child_card.dart
        ├── magic_loading_sphere.dart
        ├── magic_runic_background.dart
        ├── page_flip_animation.dart
        ├── photo_preview_grid.dart
        └── step_progress_indicator.dart
```

### Технологический стек

- **Flutter 3.10+** с Dart 3
- **State Management**: `hooks_riverpod` (Riverpod 2.5 + Flutter Hooks)
- **Routing**: `go_router` с защитой маршрутов на основе auth статуса
- **Networking**: `dio` с кастомными interceptors для JWT и обработки 401
- **Аутентификация**: `supabase_flutter` (через backend конфигурацию)
- **Модели**: `freezed` + `json_serializable` для immutable моделей с JSON сериализацией
- **UI/UX**: Material 3, кастомная дизайн-система, анимации Lottie
- **Изображения**: `cached_network_image`, `image_picker`, `flutter_image_compress`
- **Безопасность**: `flutter_secure_storage` для токенов, защита от скриншотов
- **Утилиты**: `intl`, `path`, `url_launcher`, `app_links` (deep links)

### Ключевые архитектурные решения

1. **Feature‑First организация**: Каждая фича (auth, children, books и т.д.) изолирована в своей папке с внутренним разделением на data/presentation.
2. **Слоистая архитектура**:
   - **Data Layer**: Репозитории, провайдеры, API клиенты
   - **Domain Layer**: Модели, бизнес-логика (частично в провайдерах)
   - **Presentation Layer**: Экраны, виджеты, состояние (через Riverpod)
3. **Дизайн‑система**: Полностью кастомная система дизайна (цвета, типографика, радиусы, отступы, тени) на основе Material 3.
4. **Магические компоненты**: Набор анимированных виджетов (градиенты, частицы, стеклянные карточки) для создания волшебной атмосферы.
5. **Глубокая интеграция с backend**: Автоматическое добавление JWT, обработка 401 (автоматический выход), polling статуса задач генерации с обработкой прерванных задач и поиском черновых книг.

## 🚀 Запуск и настройка

### 1. Установка зависимостей

```bash
flutter pub get
```

### 2. Генерация кода моделей (Freezed)

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. Настройка backend URL

Откройте `lib/core/api/api_client.dart` и замените `baseUrl` на адрес вашего backend:

```dart
const String baseUrl = 'http://YOUR_IP_OR_DOMAIN:8000';
```

### 4. Запуск

```bash
flutter run -d chrome          # Web
flutter run                    # Android/iOS (по умолчанию)
```

### 5. Сборка

```bash
flutter build apk              # Android APK
flutter build web              # Web
```

## 🔌 API Endpoints

Backend должен предоставлять следующие эндпоинты:

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/auth_config` | Получение конфигурации Supabase |
| POST | `/children` | Создание ребёнка (требует поле `gender`) |
| GET | `/children` | Список детей |
| PUT | `/children/:id` | Обновление ребёнка (поддерживает `gender`) |
| GET | `/books` | Список книг |
| GET | `/books/:bookId/scenes` | Сцены книги |
| POST | `/generate_full_book` | Генерация книги |
| GET | `/books/task_status/:taskId` | Статус задачи (с retry логикой для 502 ошибок) |
| POST | `/regenerate_scene` | Перегенерация сцены |
| POST | `/select_style` | Выбор стиля |
| GET | `/support/messages` | Список сообщений поддержки |
| POST | `/support/messages` | Создание сообщения поддержки |
| GET | `/support/messages/:id` | Детали сообщения поддержки |
| POST | `/support/messages/:id/replies` | Отправка ответа на сообщение |

## 📊 Модели данных

### Child (Ребенок)

```dart
{
  "id": "string",
  "name": "string",
  "age": 5,
  "gender": "male" | "female",  // Обязательное поле (по умолчанию "female" для обратной совместимости)
  "interests": "строка с интересами",
  "fears": "строка со страхами",
  "character": "описание личности",
  "moral": "мораль истории",
  "face_url": "string?",
  "photos": ["url1", "url2"]
}
```

**Примечание:** Поле `gender` является обязательным при создании нового профиля ребенка. Для существующих профилей без этого поля используется значение по умолчанию `"female"` для обеспечения обратной совместимости.

### Book (Книга)

```dart
{
  "id": "string",
  "title": "string",
  "child_id": "string",
  "user_id": "string",
  "created_at": "ISO 8601 datetime"
}
```

### Scene (Сцена)

```dart
{
  "id": "string",
  "book_id": "string",
  "order": 1,
  "short_summary": "string",
  "image_prompt": "string",
  "draft_url": "string?",
  "final_url": "string?"
}
```

### SceneVariant (Вариант сцены)

```dart
{
  "id": "string",
  "scene_id": "string",
  "variant_type": "text" | "image",
  "content": "string",
  "image_url": "string?",
  "selected": false
}
```

### SupportMessage (Сообщение поддержки)

```dart
{
  "id": "string",
  "user_id": "string",
  "subject": "string",
  "message": "string",
  "status": "open" | "closed" | "pending",
  "created_at": "ISO 8601 datetime",
  "updated_at": "ISO 8601 datetime",
  "replies": [SupportMessageReply]
}
```

### SupportMessageReply (Ответ на сообщение поддержки)

```dart
{
  "id": "string",
  "message_id": "string",
  "user_id": "string",
  "content": "string",
  "is_admin": false,
  "created_at": "ISO 8601 datetime"
}
```

## 🛡️ Особенности и решения

### Аутентификация и безопасность

- **JWT токены**: Хранятся в `FlutterSecureStorage`, автоматически добавляются в заголовки запросов через Dio interceptor.
- **Обработка 401**: При получении 401 ошибки автоматически выполняется выход и переход на экран логина.
- **Защита маршрутов**: GoRouter `redirect` проверяет `authStatusProvider` и перенаправляет неавторизованных пользователей.
- **Защита от скриншотов**: На Android включается `FLAG_SECURE` для экранов с конфиденциальной информацией.

### Производительность и UX

- **Кеширование изображений**: `cached_network_image` для эффективной загрузки и кеширования.
- **Сжатие изображений**: Загружаемые фото сжимаются до 1024px с качеством 85% перед отправкой на сервер.
- **Адаптивный UI**: Поддержка различных размеров экранов, ориентаций, плотности пикселей.
- **Плавные анимации**: Lottie анимации, кастомные анимации переходов, "магические" эффекты.
- **Улучшенная визуализация прогресса**: Белые градиенты для текста, улучшенная читаемость на экране статуса генерации.
- **Предотвращение переполнения текста**: Использование `Flexible`, `ConstrainedBox` и `SingleChildScrollView` для корректного отображения длинных текстов.
- **Таймер для черновых изображений**: Отображение обратного отсчета времени генерации черновых изображений в книге.

### Кроссплатформенность

- **Android/iOS/Web**: Единая кодовая база.
- **Deep Links**: Обработка ссылок `storyhero://` и `https://` через `app_links`.
- **Адаптация под платформу**: Учет особенностей каждой платформы в UI и поведении.

### Система поддержки

- **Сообщения поддержки**: Пользователи могут создавать обращения в службу поддержки через приложение.
- **Ответы администраторов**: Поддержка диалогов с ответами от администраторов.
- **Статусы сообщений**: Отслеживание статуса обращения (открыто, закрыто, в обработке).
- **История переписки**: Просмотр полной истории переписки по каждому обращению.

### Отказоустойчивость

- **Обработка ошибок**: Централизованный error handling через `error_analyzer.dart`.
- **Повторные попытки**: Для критичных операций (генерация книги) реализован polling статуса с retry логикой для 502 ошибок.
- **Обработка отсутствующих полей**: Автоматическое добавление недостающих полей (например, `id` в TaskStatus из URL) для совместимости с backend.
- **Обратная совместимость**: Поддержка старых данных без обязательных полей (например, `gender` в Child модели с дефолтным значением).
- **Graceful degradation**: При отсутствии интернета показываются понятные сообщения.

## 🆕 Последние изменения (Web + UI/UX)

### Web‑адаптация и кроссплатформенность

- **Единые файловые операции**: кроссплатформенный выбор и загрузка изображений (web + mobile) через `XFile` и `MultipartFile.fromBytes`.
- **StorageService**: единый интерфейс хранения токенов для mobile (`FlutterSecureStorage`) и web (`SharedPreferences`).
- **ImageCompressor**: на web возвращает исходный файл (без компрессии), на mobile сжатие сохранено.
- **Web SEO**: расширены мета‑теги `web/index.html`, добавлены `web/robots.txt` и `web/sitemap.xml`.

### Десктопные макеты и ограничения ширины

- **DesktopContainer**: единый компонент центрирования и ограничения ширины контента на больших экранах.
- Применён на ключевых экранах (Home, My Books, Children, Create Book, Book View, Settings, Help, Subscription, Task Status и т.д.).

### UI‑полировка и читабельность

- Улучшены **градиенты и контраст текста** (в том числе на экранах статуса генерации).
- Устранено **переполнение текста** в карточках и сценах (гибкие блоки, ограничения высоты, прокрутка).
- Экран **Книги ребёнка** приведён к формату карточек “Мои книги”: крупные обложки, статус‑бейдж, единая структура.
- Карточки выбора стилей на экране генерации стали **компактнее и без прокрутки** (адаптивная сетка).

### Формы и модели

- Добавлено обязательное поле **пол ребёнка** в анкете (gender) с защитой обратной совместимости при парсинге.
- Парсеры API стали устойчивее к разным форматам ответов (например, при финальной генерации книги).

### Производительность и современный web‑вид

- Убраны “бесконечные” glow‑анимации на web (оставлен мягкий статичный эффект).
- Добавлены **аккуратные hover‑эффекты** для карточек и кнопок на desktop.
- Улучшена читаемость форм: более плотный фон для полей на web.

## 🔧 Утилиты и сервисы

### DeepLinkService

Обрабатывает deep links для навигации внутри приложения. Поддерживает:
- Инициализацию при открытии по ссылке
- Обработку ссылок во время работы приложения
- Преобразование схемы `storyhero://` во внутренние пути

### ScreenshotProtectionService

Включает флаг `FLAG_SECURE` для окон, чтобы запретить скриншоты и запись экрана (только Android).

### ImageCompressor

Сжимает изображения перед загрузкой на сервер для экономии трафика и ускорения загрузки.

### ImpellerGuard

Отключает Impeller на устройствах Realme/Oppo/Oplus, где он вызывает краши. Основное отключение через мета-данные в `AndroidManifest.xml`, этот класс предоставляет дополнительную защиту.

### PhoneFormatter

Утилита для форматирования телефонных номеров в единый формат. Используется в полях ввода телефона для обеспечения консистентности данных.

### AddressInputFields и PhoneInputField

Специализированные виджеты для ввода адреса и телефона с валидацией и форматированием. Обеспечивают единообразный UX при работе с контактными данными.

## 🎨 Дизайн-система

### Цветовая палитра (AppColors)

- `primary` (#7C3AED) — основной фиолетовый
- `secondary` (#10B981) — зеленый акцент
- `tertiary` (#F59E0B) — оранжевый акцент
- `surface` (#1E293B) — поверхность карточек
- `background` (#0F172A) — фон приложения
- `error` (#EF4444) — ошибки

### Типографика (AppTypography)

Использует `GoogleFonts.plusJakartaSans` с четкой иерархией:
- `displayLarge`, `displayMedium` — заголовки экранов
- `headlineLarge`, `headlineMedium`, `headlineSmall` — секции
- `bodyLarge`, `bodyMedium`, `bodySmall` — основной текст
- `labelLarge`, `labelMedium`, `labelSmall` — кнопки, метки

### Радиусы (AppRadius)

- `radiusSM` (8.0)
- `radiusMD` (12.0)
- `radiusLG` (16.0)
- `radiusXL` (24.0)
- `radius2XL` (32.0)

### Отступы (AppSpacing)

Система 4px базовой единицы:
- `paddingXS` (4.0)
- `paddingSM` (8.0)
- `paddingMD` (12.0)
- `paddingLG` (16.0)
- `paddingXL` (20.0)
- `padding2XL` (24.0)
- `padding3XL` (32.0)
- `padding4XL` (40.0)

### Тени (AppShadows)

Три уровня теней для создания глубины:
- `shadowSM` — легкая тень для карточек
- `shadowMD` — средняя тень для всплывающих окон
- `shadowLG` — глубокая тень для модальных окон

## 📝 TODO и планы развития

### Высокий приоритет
- [x] Редактирование профилей детей (включая поле gender)
- [x] Удаление книг
- [x] Система поддержки (сообщения и ответы)
- [ ] Загрузка изображений на сервер (получение faceUrl)
- [ ] Функция "Поделиться книгой"
- [ ] Экспорт книги в PDF

### Средний приоритет
- [ ] Push-уведомления о завершении генерации
- [ ] Офлайн-режим (кеширование книг)
- [ ] Поддержка нескольких языков (i18n)
- [ ] A/B тестирование UI компонентов
- [ ] Интеграция аналитики (Firebase Analytics)

### Низкий приоритет
- [ ] Голосовое чтение книг (TTS)
- [ ] Интерактивные элементы в книгах
- [ ] Социальные функции (публичные библиотеки)
- [ ] Генерация видео-книг

## 🧪 Тестирование

Проект включает базовую структуру для тестирования:

```bash
# Запуск unit тестов
flutter test

# Запуск интеграционных тестов
flutter test integration_test/
```

### Тестовое покрытие
- **Unit тесты**: Модели, утилиты, провайдеры
- **Widget тесты**: Ключевые виджеты дизайн-системы
- **Integration тесты**: Критичные пользовательские сценарии

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Стиль кода
- Следуйте [Dart Style Guide](https://dart.dev/guides/language/effective-dart/style)
- Используйте `flutter analyze` для проверки качества кода
- Генерируйте модели через `build_runner` перед коммитом

## 📄 Лицензия

Этот проект лицензирован под MIT License — см. файл [LICENSE](LICENSE) для деталей.

## 📞 Контакты

По вопросам разработки и сотрудничества обращайтесь к команде StoryHero.

---
*Последнее обновление: январь 2025*
